<section class="profile-background-f">
  <div id="content" class="nav-spacing">
    <h1 style="text-align: center;" class="profile-h1 col-md-12 style-white">Friends</h1>
    <div class="row profile-campaings-box">
      <% if @users.any? %>
        <% @users.each do |user| %>
          <% if current_user != user %>
            <% if !current_user.following?(user) %>
              <div class="col-md-3">
                <div class="profile-create-card">
                  <%# <%= form_for(current_user.active_relationships.build) do |f| %>
                    <%# <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                    <%# <%= hidden_field_tag :followed_id, user.id %>
                    <!-- <div style="font-size: 2em;" class="profile-input-padding"> -->
                      <%# <%= f.submit "Follow " + user.email, class: "profile-create-card", remote: true %>
                    <!-- </div> -->
                  <%# <% end %>
                  <a href="/profile/<%= user.id %>" class="style-white">
                    <%= image_tag("user_placeholder.jpg", style: "width:100%") %>
                    <h1 class="profile-overlay"><%= user.email %></h1>
                  </a>
                </div>
              </div>
            <% else %>
              <div class="col-md-3">
                <div class="profile-create-card">
                  <%# <%= form_for(current_user.active_relationships.find_by(followed_id: user.id), html: { method: :delete }, remote: true) do |f| %>
                    <%# <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                    <!-- <div style="font-size: 2em;" class="profile-input-padding"> -->
                      <%# <%= f.submit "Unfollow " + user.email, class: "profile-create-card", remote: true %>
                    <!-- </div> -->
                  <%# <% end %>
                  <a href="/profile/<%= user.id %>" class="style-white">
                    <%= image_tag("user_placeholder.jpg", style: "width:100%") %>
                    <h1 class="profile-overlay"><%= user.email %></h1>
                  </a>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>
</section>
